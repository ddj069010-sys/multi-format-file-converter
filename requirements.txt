# requirements.txt - Dependencies for Universal File Compression Suite

# Web Framework (Required)
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
pydantic>=2.0.0

# Image Processing (Required for image conversion)
Pillow>=10.1.0

# Document Processing (Optional - for DOCX support)
python-docx>=0.8.11

# PDF Processing (Optional - for PDF operations)
pypdf>=3.17.0

# Archive Handling (Optional - for 7Z support)
py7zr>=0.20.8

# Note: The following are system packages and must be installed separately:
# - FFmpeg: Required for audio/video conversion
#   Install: apt-get install ffmpeg (Ubuntu/Debian) or brew install ffmpeg (macOS)
# - Pandoc: Optional for advanced document conversion
#   Install: apt-get install pandoc (Ubuntu/Debian) or brew install pandoc (macOS)

# PDF Processing (Optional - for PDF merge operations)
pypdf>=3.17.0

# Optional dependencies (commented out - uncomment if needed)
# python-pptx>=0.6.21          # For PowerPoint support
# openpyxl>=3.11.0             # For Excel support
# pandas>=2.1.2                # For data processing